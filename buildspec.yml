version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Node.js code

artifacts:
  files:
    - '**/*'
  discard-paths: no
  base-directory: ./